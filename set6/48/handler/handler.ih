#include "handler.h"

#include <iostream>

using namespace std;

// by element.cc kill.cc sum.cc fill.cc
inline bool Handler::available(size_t nRequired)
{
    return specified(nRequired) and connect();
}
