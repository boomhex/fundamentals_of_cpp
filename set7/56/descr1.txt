This program reads a binary Linux process accounting file (pacct) and extracts
all recorded process information. It builds a string of formatted records,
to see program terminations, exit codes, and system activity.
