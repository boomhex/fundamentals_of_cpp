// Own includes:
#include "pacctfile/pacctfile.h"

// cpp includes:
#include <iostream>
#include <string>

using namespace std;

bool allFlag(size_t argc, char **argv);
string inputPath(size_t argc, char **argv);
