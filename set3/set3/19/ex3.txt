3:
It is not appropriate to define a const reference to an int-type parameter
because an int is a built-in type. Handling the actual type is faster than
handling a built-in type. So that is therefore the preferred alternative.
